# Hugging Face token can be entered in the browser when generating (never stored),
# or set here as a server-side fallback:
# HF_TOKEN=hf_…
SF3D_SPACE_ID=Upsampler/stable-fast-3d
SF3D_API_NAME=/image_to_glb
